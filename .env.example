# Production Environment Variables Example
# Copy this file to .env and fill in the actual values
# IMPORTANT: Never commit the actual .env file to version control

# Database Connection (PostgreSQL on GCP)
DB_CONNECTION_STRING=Host=35.187.209.229;Port=5432;Database=postgres;Username=postgres;Password=YOUR_DB_PASSWORD

# JWT Configuration
# Generate a secure key: openssl rand -base64 32
JWT_SIGNING_KEY=CHANGE_ME_TO_A_SECURE_256BIT_SECRET_KEY

# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY_HERE
OPENAI_MODEL=gpt-4o-mini

# Encryption Key for API Key Storage
# Generate: openssl rand -base64 32
KEYRING_MASTER_KEY=CHANGE_ME_TO_A_BASE64_ENCODED_32_BYTE_KEY