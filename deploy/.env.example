# Environment Variables for Health API Deployment
# Copy this file to .env and fill in the actual values

# Security Keys (REQUIRED - Generate secure keys for production!)
KEYRING_MASTER_KEY=base64_encoded_32_byte_key_here
JWT_SIGNING_KEY=your_secure_jwt_signing_key_at_least_32_chars

# Database Configuration (GCP Cloud SQL)
DB_HOST=35.187.209.229
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=Ldtech@4649
DB_SCHEMA=health

# GCP Configuration
GCP_PROJECT_ID=ldtech
GCP_REGION=asia-northeast1
GCP_ZONE=asia-northeast1-a

# SSL Certificate (for HTTPS)
SSL_CERT_PATH=/etc/ssl/certs/ldetch.co.jp.pfx
SSL_CERT_PASSWORD=your_certificate_password

# OpenAI API (Optional - can be configured via admin panel)
OPENAI_API_KEY=sk-your-openai-api-key

# Application Settings
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:5000;https://+:5001